[package]
name = "parser"
version = "0.1.0"
edition.workspace = true

[dependencies]
protocol.workspace = true

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "state_machine"
harness = false
